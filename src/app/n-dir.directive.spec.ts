import { NDirDirective } from './n-dir.directive';

describe('NDirDirective', () => {
  it('should create an instance', () => {
    const directive = new NDirDirective();
    expect(directive).toBeTruthy();
  });
});
