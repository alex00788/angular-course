import { DirektivDirective } from './direktiv.directive';

describe('DirektivDirective', () => {
  it('should create an instance', () => {
    const directive = new DirektivDirective();
    expect(directive).toBeTruthy();
  });
});
